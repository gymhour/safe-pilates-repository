{
  "name": "gymhour-template",
  "private": true,
  "scripts": {
    "dev": "npm run dev:api & npm run dev:front",
    "dev:api": "npm --prefix api-gymhour run dev",
    "dev:front": "npm --prefix front-gymhour start",
    "install:all": "npm install --prefix api-gymhour && npm install --prefix front-gymhour"
  }
}
